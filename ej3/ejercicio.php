<?php

/*
 * Dado el nombre de un fichero pasado:
 *   Si la extensión termina en .exe debe devolver el nombre del fichero en mayúsculas
 *   Si la extensión termina en .db debe devolver el nombre del fichero en minusculas
 *   Si no cumple ninguna de las 2 anteriores reglas, debe devolver el mismo nombre del fichero
 *
 */


function extension(string $numero): string {
    return '';
}
